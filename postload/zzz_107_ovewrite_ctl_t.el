;;; postload/zzz_107_ovewrite_ctl_t.el -*- lexical-binding: t; -*-

;; override does not work?
;; (global-set-key (kbd "C-t") 'org-todo)

;; dirty workaround ...
;; DOES NOT WORK EITHER
;; (defun +worskpace/new ()
;;   "Do not create new workspace because it breaks my emacs."
;;   (interactive)
;;   (message "Fortunately workspaces are alread disabled. You are safe now."))
