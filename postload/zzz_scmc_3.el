;;; postload/zzz_scmc_3 -*- lexical-binding: t; -*-

(use-package! el-sc-tweaks
 ;; :when (and (modulep! +el_sc) (modulep! +el_sc_tweaks))
  :after el-sc
  ;; :init
  ;; « ref_el-sc-tweaks-el_sc_tweaks-init»
  ;; :config
  ;; « ref_el-sc-tweaks-el_sc_tweaks-config»
  )
